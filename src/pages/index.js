import * as React from 'react'
import Layout from '../components/layout'

const IndexPage = () => {
  return (
    <Layout pageTitle="rambler's blog">
      <p>Rambling about some web technologies without actually talking tech-y. So, more like how I feel about them while writing the code...</p>
    </Layout>
  )
}
export default IndexPage